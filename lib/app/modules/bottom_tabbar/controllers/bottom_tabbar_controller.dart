import 'package:smart_billing_app/app/modules/dashboard/views/dashboard_view.dart';
import 'package:smart_billing_app/constant/import.dart';

class BottomTabbarController extends GetxController {
  // RxInt currentIndex = 2.obs; // Dashboard default (2 - Reports)

  // // Pages ko controller ke andar define karein
  // final List<Widget> pages = [
  //   Center(child: Text('Bills Page')),
  //   DashboardView(), // Index 1 par Dashboard
  //   Center(child: Text('Reports Page')),
  //   Center(child: Text('Settings Page')),
  // ];

  // void changeTab(int index) {
  //   if (index >= 0 && index < pages.length) {
  //     currentIndex.value = index;
  //   }
  // }
  
  // Widget getCurrentPage() {
  //   return pages[currentIndex.value];
  // }
}